// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver')
const assert = require('assert')

describe('TC25a - List of selectable food categories are displayed', function() {
  this.timeout(30000)
  let driver
  let vars
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build()
    vars = {}
  })
  afterEach(async function() {
    await driver.quit();
  })
  it('TC25a - List of selectable food categories are displayed', async function() {
    await driver.get("http://onlinecafeteria.com/index.html")
    await driver.findElement(By.id("ViewPreferencesBtn")).click()
    {
      const elements = await driver.findElements(By.xpath("//form[@id=\'order\']/div/div[4]/div/div"))
      assert(elements.length)
    }
  })
})
